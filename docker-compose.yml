---
services:
  redis:
    image: redis:7-alpine
    init: true

  devcontainer:
    image: jarppe/clj-devcontainer:latest
    restart: always
    volumes:
      - .:/home/${USER}/workspace
    command: "sleep infinity"
